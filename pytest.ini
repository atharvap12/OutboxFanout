[pytest]
testpaths = tests
# Verbose by default: these tests are a DEMO as much as a check, and the point
# is to watch each scenario's assertions go green one by one.
addopts = -v -ra --tb=short
markers =
    slow: takes tens of seconds (real SQS timers)
