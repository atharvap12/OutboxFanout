# Per-service, so each image installs only what it runs.
# Pinned to a minor range: an upgrade should be a decision, not an accident.

fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0

SQLAlchemy>=2.0,<2.1
psycopg[binary]>=3.2,<4.0     # psycopg 3, prebuilt binaries

# Listed explicitly though fastapi pulls it in, because schemas.py imports it
# directly.
pydantic>=2.9,<3.0
